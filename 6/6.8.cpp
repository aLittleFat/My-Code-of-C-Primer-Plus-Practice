#include <bits/stdc++.h>

using namespace std;

int main(int argc, char const *argv[])
{
	freopen("in", "r", stdin);
	char x;
	int cnt = 0;
	while((x = cin.get()) != EOF){
		++cnt;
	}
	cout << cnt << endl;
	fclose(stdin);
	return 0;
}